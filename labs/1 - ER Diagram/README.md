Лабораторна робота №1. Збір вимог та розробка схеми ER

Сценарій: Інформаційна система житлового агенства

Завдання:

- зібрати вимоги;
- визначити сутності та їх атрибути;
- систематизувати зв'язки між сутностями;
- описати обмеження до системи;

Вимоги: Автоматизоване середовище для координації ринку нерухомості, обліку об'єктів, обробки запитів клієнтів та юридичної фіксації угод оренди між власниками, орендарями та рієлторами.

Опис сутностей:

- Власник :
  - UUID | ID власника | PK;
  - VARCHAR | ПІБ;
  - INT | номер телефону;
  - VARCHAR | паспортні дані.
- Орендар :
  - UUID | ID орендаря | PK,
  - VARCHAR | ПІБ,
  - INT | номер телефону,
- Рієлтор :
  - UUID ID | рієлтора | PK,
  - VARCHAR | ПІБ,
  - INT | телефон,
  - DECIMAL | відсоток комісійних.
- Об'єкт нерухомості :
  - UUID | ID об'єкта | PK,
  - UUID | ID власника | FK,
  - VARCHAR | адреса,
  - Text | статус (доступно/орендовано).
- Заявка :
  - UUID | ID заявки | PK,
  - UUID | ID орендаря | FK,
  - UUID | ID об'єкта | FK,
  - DATE | дата подачі,
  - Text | статус (в обробці/схвалено/відхилено).
- Угода оренди :
  - UUID | ID угоди | PK,
  - UUID | ID об'єкта | FK,
  - UUID | ID орендаря | FK,
  - UUID | ID рієлтора | FK,
  - DATE | дата початку,
  - DATE | дата закінчення,
  - DECIMAL | сума оренди,
  - DECIMAL | комісія агентства.

Зв'язки між сутностями:

- Власник — Об'єкт нерухомості (1 : N): Один власник може володіти кількома об'єктами нерухомості, але кожен об'єкт належить лише одному власникові.
- Орендар — Заявка (1 : N): Один орендар може подавати декілька заявок на перегляд або оренду різних об'єктів.
- Об'єкт нерухомості — Заявка (1 : N): На один об'єкт нерухомості може бути подано декілька заявок від різних орендарів.
- Орендар — Угода оренди (1 : N): Один орендар може виступати стороною в кількох угодах оренди (наприклад, у різний час).
- Об'єкт нерухомості — Угода оренди (1 : N): Один об'єкт нерухомості може фігурувати в багатьох угодах оренди протягом усього часу роботи агентства.
- Рієлтор — Угода оренди (1 : N): Один рієлтор може супроводжувати та закривати багато угод оренди.

Код ER діаграми (mermaid):

erDiagram
PROPERTY_OWNER ||--o{ PROPERTY : "owns"
TENANT ||--o{ APPLICATION : "submits"
PROPERTY ||--o{ APPLICATION : "receives"
TENANT ||--o{ LEASE_AGREEMENT : "signs"
PROPERTY ||--o{ LEASE_AGREEMENT : "is subject of"
REALTOR ||--o{ LEASE_AGREEMENT : "manages"

    PROPERTY_OWNER {
        uuid owner_id PK
        varchar full_name
        varchar phone_number
        varchar passport_data
    }

    TENANT {
        uuid tenant_id PK
        varchar full_name
        varchar phone_number
    }

    REALTOR {
        uuid realtor_id PK
        varchar full_name
        varchar phone_number
        decimal commission_rate
    }

    PROPERTY {
        uuid property_id PK
        uuid owner_id FK
        varchar address
        text status
    }

    APPLICATION {
        uuid application_id PK
        uuid tenant_id FK
        uuid property_id FK
        date submission_date
        text status
    }

    LEASE_AGREEMENT {
        uuid lease_id PK
        uuid property_id FK
        uuid tenant_id FK
        uuid realtor_id FK
        date start_date
        date end_date
        decimal rental_amount
        decimal agency_commission
    }

Обмеження до системи:

- Унікальність даних: Номери телефонів та email для Власників, Орендарів і Рієлторів повинні бути унікальними в системі.
- Валідація числових значень:
  - Сума оренди та комісія агентства мають бути суворо більшими за 0.
  - Відсоток комісійних рієлтора має перебувати в межах від 0% до 100%.
- Логіка дат: Дата закінчення угоди оренди повинна бути пізнішою за дату початку (`end_date > start_date`).
- Бізнес-правила статусів:
  - Об'єкт нерухомості не може мати двох активних угод оренди на один і той самий період.
  - При укладанні угоди статус об'єкта змінюється на `орендовано`, а відповідна заявка — на `схвалено`.
